anthropic>=0.25.0
ultralytics>=8.0.0      # for yolo_detect tool
pyyaml>=6.0
openai>=1.0.0       # for vLLM / OpenAI-compat backend
requests>=2.31.0    # for Ollama backend
flask>=3.0.0        # optional: for Slack trigger listener
slack-bolt>=1.18.0  # optional: for Slack Socket Mode scheduler (pulls in slack-sdk, websocket-client)
